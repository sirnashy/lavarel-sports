services:
  # Web Service
  - type: web
    name: sportstream-web
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: LOG_CHANNEL
        value: stderr
      - key: DB_CONNECTION
        value: mysql
      - key: CACHE_DRIVER
        value: redis
      - key: SESSION_DRIVER
        value: database
      - key: QUEUE_CONNECTION
        value: redis
      - key: REDIS_URL
        fromService:
          type: redis
          name: sportstream-redis
          property: connectionString

  # Queue Background Worker
  - type: worker
    name: sportstream-queue
    env: docker
    dockerfilePath: Dockerfile
    startCommand: php artisan queue:work --verbose --tries=3 --timeout=90
    envVars:
      - key: APP_ENV
        value: production
      - key: DB_CONNECTION
        value: mysql
      - key: CACHE_DRIVER
        value: redis
      - key: QUEUE_CONNECTION
        value: redis
      - key: REDIS_URL
        fromService:
          type: redis
          name: sportstream-redis
          property: connectionString

  # Redis Cache & Queue Store
  - type: redis
    name: sportstream-redis
    ipAllowList: [] # Only accessible by services within the same group
